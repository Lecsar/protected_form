import {makeStyles} from '@material-ui/core';

export const useFormStyles = makeStyles({
    form: {
        flexGrow: 1,
        // height: '100vh',
        // backgroundColor: '#e0e0e0',
    },

    header: {
        fontSize: '3rem',
        textAlign: 'center',
        margin: '1rem 0',
    },
});
