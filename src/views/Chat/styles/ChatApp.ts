import {makeStyles} from '@material-ui/styles';

export const useStylesChatApp = makeStyles({
    root: {
        flexGrow: 1,
        height: 1000,
    },

    header: {
        fontSize: '3rem',
        textAlign: 'center',
        margin: '1rem 0',
    },

    
});
