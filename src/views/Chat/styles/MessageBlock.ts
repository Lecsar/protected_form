import {makeStyles} from '@material-ui/styles';

export const useStylesMessageBlock = makeStyles({
    chat: {
        backgroundColor: '#f5f5f5',
        height: '70rem',
        overflow: 'auto',
    },
});
