import {makeStyles} from '@material-ui/styles';

export const useCabinetStyles = makeStyles({
    header: {
        fontSize: '3rem',
        textAlign: 'center',
        margin: '1rem 0',
    },
});
