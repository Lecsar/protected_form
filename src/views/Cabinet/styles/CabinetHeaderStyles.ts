import {makeStyles} from '@material-ui/styles';

export const useCabinetStyles = makeStyles({
    appbar: {
        display: 'flex',
        alignItems: 'center',
        marginBottom: '2rem',
    },

    toolbar: {
        padding: 0,
    },
});
