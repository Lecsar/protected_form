import {makeStyles} from '@material-ui/styles';

export const useListStyles = makeStyles({
    checkBox: {
        fontSize: '2rem',
    },

    text: {
        display: 'flex',
    },

    primary: {
        fontSize: '1.7rem',
    },
});
