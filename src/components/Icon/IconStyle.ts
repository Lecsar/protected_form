import {makeStyles} from '@material-ui/styles';

export const useIconStyles = makeStyles({
    icon: {
        fontSize: '2rem',
    },

    spinner: {
        width: '2rem',
        height: '2rem',
    },

    check: {
        color: '#1faa00',
    },

    error: {
        color: '#c62828',
    },
});
