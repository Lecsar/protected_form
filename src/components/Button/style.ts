import {makeStyles, createStyles} from '@material-ui/core';

export default makeStyles(
    createStyles({
        root: {
            fontSize: '2rem',
            margin: '10px 0',
        },
    }),
);
